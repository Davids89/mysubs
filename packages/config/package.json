{
  "name": "@mysubs/config",
  "private": true,
  "type": "module",
  "exports": {
    "./tsconfig-base": "../../tsconfig.base.json"
  },
  "scripts": {
    "build": "echo \"No config build needed\"",
    "lint": "echo \"No config lint needed\"",
    "test": "echo \"No config tests needed\"",
    "typecheck": "echo \"No config typecheck needed\""
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
