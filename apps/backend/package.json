{
  "name": "@subtrack/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @subtrack/shared-types build && prisma generate && tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "db:deploy": "prisma migrate deploy",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "lint": "eslint src spec --max-warnings=0",
    "start": "node dist/main.js",
    "test": "pnpm --filter @subtrack/shared-types build && vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm --filter @subtrack/shared-types build && DATABASE_URL=postgresql://subtrack:subtrack@localhost:5432/subtrack?schema=public prisma generate && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@subtrack/shared-types": "workspace:^",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "express": "latest",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.21.0",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/express": "latest",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "latest",
    "@types/supertest": "latest",
    "prisma": "^7.8.0",
    "supertest": "latest",
    "tsx": "latest",
    "typescript": "^5.9.3",
    "vitest": "latest"
  }
}
